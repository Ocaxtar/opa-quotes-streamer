global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'opa-quotes-streamer'
    static_configs:
      - targets: ['streamer:8001']
        labels:
          service: 'opa-quotes-streamer'
          environment: 'dev'
